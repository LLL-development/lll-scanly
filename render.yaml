services:
  - type: web
    name: scanly-api
    env: docker
    region: oregon
    plan: starter
    dockerfilePath: Dockerfile
    dockerContext: .
    envVars:
      - key: NODE_ENV
        value: production
